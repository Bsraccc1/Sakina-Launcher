<?xml version="1.0" encoding="utf-8"?><!--
  Inline rename row for one app-drawer row. Inflated on demand from the ViewStub in
  adapter_app_drawer.xml — an AppCompatEditText is expensive to construct, so it must
  not exist on rows the user is only scrolling past.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/renameLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:focusable="true"
    android:gravity="end|center_vertical"
    android:orientation="horizontal">

    <androidx.appcompat.widget.AppCompatEditText
        android:id="@+id/etAppRename"
        style="@style/TextLarge"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="8dp"
        android:layout_weight="1"
        android:background="@null"
        android:gravity="center_vertical"
        android:inputType="textCapWords"
        android:maxLength="30"
        android:maxLines="1"
        android:paddingVertical="@dimen/app_padding_vertical"
        android:textColorHint="?attr/primaryColorTrans50" />

    <TextView
        android:id="@+id/tvSaveRename"
        style="@style/TextSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:drawablePadding="2dp"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:paddingHorizontal="8dp"
        android:text="@string/rename"
        android:textSize="12sp"
        app:drawableTopCompat="@drawable/ic_check" />

    <TextView
        android:id="@+id/appRenameClose"
        style="@style/TextSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawablePadding="2dp"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:paddingHorizontal="8dp"
        android:text="@string/close"
        android:textSize="12sp"
        app:drawableTopCompat="@drawable/ic_close" />
</LinearLayout>
