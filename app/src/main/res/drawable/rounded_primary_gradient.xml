<?xml version="1.0" encoding="utf-8"?>
<!--
  Legacy name (no gradient inside, and none wanted). Settings cards use the same
  glass card surface as Muslim Center and Productive so the whole app matches.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/glassRaised" />
    <corners android:radius="20dp" />
    <stroke
        android:width="1dp"
        android:color="?attr/glassHairline" />
</shape>
